document.addEventListener('DOMContentLoaded', () => {
    const featuresSection = document.querySelector('.features');
    featuresSection.classList.add('visible'); // Add the class to trigger the fade-in
});
